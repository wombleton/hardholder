tr.title
  td.condition
    a(href=move.url) #{move.condition}
  td.details
    .date=move.date
    !=partial('controls', { object: move.meta })
  td.stat
    +#{move.stat}
tr.def
  td.definition(colspan=3)!=move.definition
tr
  td(colspan=3)
    a(href=move.edit_url) Edit